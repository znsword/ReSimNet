# ReSimNet 源码结构分析文档计划

## 文档位置与名称
- 在项目根目录创建 `SOURCE_STRUCTURE.md`

## 文档章节结构
- 项目概述：简述目标、核心思想（Siamese 网络预测药物响应相似度）
- 目录结构：列出顶层目录与关键文件，描述职责
- 核心模块：
  - 模型定义（`models/drug_model.py`）：架构、前向、距离层、图卷积分支
  - 数据集与加载（`tasks/drug_task.py`）：数据处理、表示类型、DataLoader/Collate
  - 训练与评估（`tasks/drug_run.py`）：回归/二分类循环、评估指标、结果保存
  - 程序入口（`main.py`）：参数、运行流程、模型构造
  - 工具与可视化（`utils.py`、`tasks/plot.py`、`tasks/run_plot.py`）：辅助函数与TSNE绘图
- 运行脚本：`train.sh`、`train_ensemble.sh`、`predict*.sh` 等用途与调用参数
- 数据与依赖：所需数据文件、预训练模型、外部依赖（PyTorch 0.3 等）
- 参数说明：`rep_idx`、`binary`、学习率、嵌入维度、图模型参数等
- 典型工作流：训练→验证→测试；预测与嵌入导出；ZINC打分与集成
- 潜在问题与改进建议：
  - `models.root.utils` 路径缺失的潜在导入问题与修正建议
  - 旧版 PyTorch API（`Variable`、`F.sigmoid`）兼容性提示

## 实现方式
- 依据现有源码与文档，编写完整 Markdown 文本，嵌入代码引用（`file_path:line_number`）
- 保持简洁、可扫描的结构与要点式说明

## 确认请求
- 若你确认上述结构与位置，我将创建 `SOURCE_STRUCTURE.md` 并填入完整分析内容。